name: Belajar Secrets

on:
  push:
    branches:
      - master

jobs:
  Belajar:
    name: Belajar Penggunaan Environment Secrets
    runs-on: ubuntu-latest
    environment: Development
    
    steps:
      - name: Print Environment Secrets
        run: |
          echo "${{ env.MATERI_6 }}" > MATERI_6.txt
          
      - name: Run tmate
        uses: mxschmitt/action-tmate@v2
