name: Belajar Secrets

on:
  push:
    branches:
      - master

jobs:
  testing:
    name: Test Penggunaan Environment Secrets
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Print Environment Secrets
        run: |
          echo "${{ secrets.MATERI_6 }}"
